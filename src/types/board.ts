export interface BoardType {
  id: number;
  destination: string;
  period: string;
  mainTitle: string;
  subTitle: string;
  coverImage: string;
  profileImage: string;
  nickname: string;
  styles: string[];
}
