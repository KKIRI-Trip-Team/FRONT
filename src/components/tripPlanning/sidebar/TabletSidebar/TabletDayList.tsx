'use client';

import DayListButton from '../button/DayListButton';

export default function TabletDayList() {
  return (
    <section className="flex w-[768px] px-[20px] py-[16px] items-center gap-[10px]">
      <DayListButton />
    </section>
  );
}
